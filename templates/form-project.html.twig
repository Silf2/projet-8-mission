{% block form_project %}
    <div id="content">
        {{ form_start(form) }}
            <div>
                {{ form_row(form.name) }}
            </div>
            <div>
                {{ form_row(form.users) }}       
            </div>
            <br>
            <button class="button button-submit">Continuer</button>
        {{ form_end(form) }}
    </div>
{% endblock %}